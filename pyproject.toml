[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "jira-md-upload"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["requests"]

[project.scripts]
jira-md-upload = "jira_md_upload:main"
korean-spacing = "korean_spacing:main"
